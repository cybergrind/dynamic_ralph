[
  {
    "operation": "skip",
    "target_step_id": "step-004",
    "reason": "No test architecture needed — existing tests should continue to pass, no new test patterns required"
  },
  {
    "operation": "split",
    "target_step_id": "step-005",
    "new_steps": [
      {
        "type": "coding",
        "description": "Phase 1 - Infrastructure: Fix pyproject.toml (name, add pydantic dep, fix isort known-first-party, remove alembic exclude), expand .gitignore, create docker/Dockerfile (standalone, python:3.13-slim base), create README.md, create CLAUDE.md"
      },
      {
        "type": "coding",
        "description": "Phase 2 - Decouple from parent project: Make constants.py configurable via env vars with generic defaults, replace BASE_AGENT_INSTRUCTIONS in prompts.py with generic guidance (target project reads its own CLAUDE.md), fix Dockerfile path in docker.py to use docker/Dockerfile, change git emails from octobrowser.net to generic default in run_dynamic_ralph.py and executor.py, make PREPARE_SYSTEM_PROMPT generic"
      }
    ],
    "reason": "Split implementation into infrastructure creation + parent-project decoupling for cleaner scope"
  },
  {
    "operation": "skip",
    "target_step_id": "step-008",
    "reason": "Review integrated into final_review for this migration task"
  },
  {
    "operation": "skip",
    "target_step_id": "step-009",
    "reason": "No test pruning needed — not adding new tests"
  }
]
